@model IEnumerable<FitnessCenterProject.Models.TrainerAvailability>

@{
    var trainer = ViewBag.Trainer as FitnessCenterProject.Models.Trainer;
    ViewData["Title"] = "Çalışma Saatleri";
    var gunler = new List<string> { "Pazartesi", "Salı", "Çarşamba", "Perşembe", "Cuma", "Cumartesi", "Pazar" };
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>@trainer.FullName - Çalışma Saatleri</h3>
        <a asp-controller="Trainer" asp-action="Index" class="btn btn-secondary">Eğitmenlere Dön</a>
    </div>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <div class="row">
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">Yeni Saat Ekle</div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <input type="hidden" name="TrainerId" value="@trainer.Id" />

                        <div class="mb-3">
                            <label class="form-label">Gün</label>
                            <select name="DayOfWeek" class="form-select" required>
                                @foreach (var gun in gunler)
                                {
                                    if (ViewBag.LastDay != null && (string)ViewBag.LastDay == gun)
                                    {
                                        <option value="@gun" selected>@gun</option>
                                    }
                                    else
                                    {
                                        <option value="@gun">@gun</option>
                                    }
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Başlangıç Saati</label>
                            <select name="StartTime" class="form-select" required>
                                @for (int i = 8; i <= 22; i++)
                                {
                                    string t1 = i.ToString("00") + ":00";
                                    string t2 = i.ToString("00") + ":30";

                                    if (ViewBag.LastStart != null && (string)ViewBag.LastStart == t1)
                                    {
                                        <option value="@t1" selected>@t1</option>
                                    }
                                    else
                                    {
                                        <option value="@t1">@t1</option>
                                    }

                                    if (ViewBag.LastStart != null && (string)ViewBag.LastStart == t2)
                                    {
                                        <option value="@t2" selected>@t2</option>
                                    }
                                    else
                                    {
                                        <option value="@t2">@t2</option>
                                    }
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Bitiş Saati</label>
                            <select name="EndTime" class="form-select" required>
                                @for (int i = 9; i <= 23; i++)
                                {
                                    string t1 = i.ToString("00") + ":00";
                                    string t2 = i.ToString("00") + ":30";

                                    if (ViewBag.LastEnd != null && (string)ViewBag.LastEnd == t1)
                                    {
                                        <option value="@t1" selected>@t1</option>
                                    }
                                    else
                                    {
                                        <option value="@t1">@t1</option>
                                    }

                                    if (ViewBag.LastEnd != null && (string)ViewBag.LastEnd == t2)
                                    {
                                        <option value="@t2" selected>@t2</option>
                                    }
                                    else
                                    {
                                        <option value="@t2">@t2</option>
                                    }
                                }
                            </select>
                        </div>

                        <button type="submit" class="btn btn-success w-100">+ Ekle</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-light">Mevcut Müsaitlik Durumu</div>
                <div class="card-body">
                    @if (!Model.Any())
                    {
                        <p class="text-muted text-center my-3">Henüz çalışma saati eklenmemiş.</p>
                    }
                    else
                    {
                        <table class="table table-bordered table-striped text-center align-middle">
                            <thead>
                                <tr>
                                    <th>Gün</th>
                                    <th>Saat Aralığı</th>
                                    <th>İşlem</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.DayOfWeek</td>
                                        <td>
                                            <span class="badge bg-success fs-6">
                                                @item.StartTime.ToString(@"hh\:mm") - @item.EndTime.ToString(@"hh\:mm")
                                            </span>
                                        </td>
                                        <td>
                                            <form asp-action="Delete" method="post" class="d-inline">
                                                <input type="hidden" name="id" value="@item.Id" />
                                                <button type="submit" class="btn btn-sm btn-danger">Sil</button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
</div>